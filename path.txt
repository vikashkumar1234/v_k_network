1. data is stored in the form of stream of snapshots.
2. Takes a snapshots whenever a changes is performed or saved.
3. git config --list
4. git config --global user.name "vikash.kumar"
   git config --global user.email "vikash.aa.kumar@accenture.com"

5. git clone path(it will clone the project in your local)
   git checkout -b branch_name(it will create the new branch in your reposity)
   git merge develop(this command used to merge your changes whatever u did in ur develop branch, and it will merge in ur master branch)
   git push(it will pushed your changes in your repositry)
   git checkout branch_name(will move to the particular branch)
   git commit -a -m "message"(will commit your changes in your local)
   